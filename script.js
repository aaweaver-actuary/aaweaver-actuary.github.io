import GithubPage from './github_page';

// create a new instance of the GithubPage class
const gh_page = new GithubPage();

// get the user input from the text box
const userText = document.getElementById('userText').value;
